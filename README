1.Install Darknet
cd darknet
make -j 8
open darknet.py and change libdarknet.so's path

2.Install opencv-python
pip install opencv-python

3.Install numpy
pip install numpy

4.RUN program
python detector.py
